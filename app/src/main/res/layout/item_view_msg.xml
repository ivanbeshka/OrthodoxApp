<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="message"
            type="com.example.orthodoxapp.dataModel.Message" />
        <variable
            name="itemClickListener"
            type="com.example.orthodoxapp.ui.msgs.ItemClickListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:onClick="@{() -> itemClickListener.onItemClick(message)}">

        <ImageView
            android:id="@+id/iv_addressee_avatar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_avatar_svgrepo_com"/>

        <TextView
            android:id="@+id/tv_msg_addressee"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@{message.addresseeName}"
            app:layout_constraintStart_toEndOf="@+id/iv_addressee_avatar"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_dialog_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            android:text="@{message.textMessage}"
            app:layout_constraintStart_toEndOf="@+id/iv_addressee_avatar"
            app:layout_constraintTop_toBottomOf="@+id/tv_msg_addressee" />

        <TextView
            android:id="@+id/tv_time_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{String.valueOf(message.messageTime)}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/tv_see"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_time_msg"
            app:srcCompat="@drawable/ic_remove_red_eye_black_24dp" />
    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>